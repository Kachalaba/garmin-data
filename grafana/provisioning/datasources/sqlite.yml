apiVersion: 1

datasources:
  - name: health-db
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      # Path inside the container — matches the :ro volume mount in compose.
      path: /var/lib/grafana/health.db
    editable: false
